# Oracle Instant Client 12C installation script for Linux/WSL

__INSTALL:__
- installation with argument and sudo
    - ex.
        ```sudo ./install-instantclient.sh $USER```
        
https://oracle.github.io/odpi/doc/installation.html
